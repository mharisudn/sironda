{{-- <x-layouts.app>
    <div class="bg-white p-8 rounded-2xl shadow-md text-center max-w-md w-full">
        @if ($alreadySubmitted)
            <h2 class="text-lg font-semibold text-red-600 mb-2">Kamu sudah mengisi polling ini sebelumnya!</h2>
            <p class="text-sm text-gray-700">Waktu pengisian: {{ $pollingCode->used_at->format('d M Y H:i') }}</p>
        @else
            <h2 class="text-xl font-bold text-green-700 mb-2">Terima kasih! 🎉</h2>
            <p class="text-sm text-gray-700">Pilihan kamu telah tersimpan dengan baik.</p>
        @endif
    </div>
</x-layouts.app> --}}
